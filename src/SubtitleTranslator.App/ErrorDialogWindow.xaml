<Window x:Class="SubtitleTranslator.App.ErrorDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI 字幕翻译发生错误" Width="680" Height="430" MinWidth="560" MinHeight="360"
        WindowStartupLocation="CenterScreen" Background="#F4F6FA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="应用遇到无法继续的错误" FontSize="22" FontWeight="SemiBold" Foreground="#B42318" />
        <TextBlock Grid.Row="1" Margin="0,8,0,14" TextWrapping="Wrap" Foreground="#667085"
                   Text="错误已经写入本地日志。你可以复制错误详情或打开日志目录后反馈问题。" />
        <TextBox x:Name="DetailsBox" Grid.Row="2" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" Padding="10" FontFamily="Consolas" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="复制错误详情" Click="Copy_OnClick" Padding="14,8" Margin="0,0,8,0" />
            <Button Content="打开日志目录" Click="OpenLogs_OnClick" Padding="14,8" Margin="0,0,8,0" />
            <Button Content="关闭应用" IsDefault="True" Click="Close_OnClick" Padding="14,8"
                    Background="#315EFB" Foreground="White" BorderThickness="0" />
        </StackPanel>
    </Grid>
</Window>
