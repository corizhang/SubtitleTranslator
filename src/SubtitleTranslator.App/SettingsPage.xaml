<UserControl x:Class="SubtitleTranslator.App.SettingsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Background="{StaticResource Brush.Surface.App}">
    <Grid><Grid.RowDefinitions><RowDefinition Height="{StaticResource Size.Header.Feature}"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Style="{StaticResource PageHeaderStyle}"><Grid Margin="30,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="设置" Style="{StaticResource Text.PageTitle}"/><TextBlock Text="配置新任务默认值、字幕交付和本地数据位置" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,3,0,0"/></StackPanel><Button Content="保存设置" Click="Save_OnClick" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid></Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"><Grid Margin="30,20,30,24"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="16"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel>
                <Border Style="{StaticResource CardStyle}"><StackPanel><Grid><StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="Subtitles24" Foreground="{StaticResource Brush.Brand.Default}" FontSize="24" Width="38"/><StackPanel><TextBlock Text="新任务默认偏好" Style="{StaticResource Text.SectionTitle}"/><TextBlock Text="选择视频后自动使用，仍可在工作台单独修改" Style="{StaticResource Text.Meta}"/></StackPanel></StackPanel></Grid><TextBlock Text="字幕内容" Style="{StaticResource Text.FieldLabel}" Margin="0,20,0,6"/><ComboBox ItemsSource="{Binding OutputModes}" SelectedItem="{Binding SelectedOutputMode}" Style="{StaticResource ProductComboBoxStyle}"/><TextBlock Text="源语言" Style="{StaticResource Text.FieldLabel}" Margin="0,14,0,6"/><ComboBox ItemsSource="{Binding SourceLanguages}" SelectedItem="{Binding SelectedSourceLanguage}" Style="{StaticResource ProductComboBoxStyle}"/><TextBlock Text="最终质量检查" Style="{StaticResource Text.FieldLabel}" Margin="0,14,0,6"/><ComboBox ItemsSource="{Binding QualityModes}" SelectedItem="{Binding SelectedQualityMode}" Style="{StaticResource ProductComboBoxStyle}"/><CheckBox Content="默认启用选择性 AI 翻译 QA" IsChecked="{Binding TranslationQaEnabled}" Margin="0,16,0,0"/></StackPanel></Border>
                <Border Style="{StaticResource CardStyle}" Margin="0,16,0,0"><StackPanel><StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="Database24" Foreground="{StaticResource PrimaryBrush}" FontSize="24" Width="38"/><StackPanel><TextBlock Text="本地数据与隐私" Style="{StaticResource SectionTitleStyle}"/><TextBlock Text="识别缓存和项目记录仅保存在本机" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/></StackPanel></StackPanel><TextBlock Text="项目目录" Style="{StaticResource FieldLabelStyle}" Margin="0,20,0,6"/><TextBlock Text="{Binding ProjectStoragePath}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/><ProgressBar Value="{Binding ProjectDriveUsagePercent, Mode=OneWay}" Height="7" Margin="0,14,0,0"/><TextBlock Text="{Binding ProjectStorageSummary}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,7,0,0"/><TextBlock Text="DeepSeek API Key 由 Windows DPAPI 加密；字幕正文只在翻译任务中发送给已配置的服务。" Foreground="#667085" TextWrapping="Wrap" Margin="0,16,0,0"/></StackPanel></Border>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <Border Style="{StaticResource CardStyle}"><StackPanel><StackPanel Orientation="Horizontal"><ui:SymbolIcon Symbol="FolderArrowRight24" Foreground="{StaticResource Brush.Brand.Default}" FontSize="24" Width="38"/><StackPanel><TextBlock Text="字幕交付" Style="{StaticResource Text.SectionTitle}"/><TextBlock Text="决定最终字幕保存位置和文件名" Style="{StaticResource Text.Meta}"/></StackPanel></StackPanel><TextBlock Text="输出位置" Style="{StaticResource Text.FieldLabel}" Margin="0,20,0,6"/><ComboBox ItemsSource="{Binding PublishLocations}" SelectedItem="{Binding SelectedPublishLocation}" Style="{StaticResource ProductComboBoxStyle}"/><TextBlock Text="命名策略" Style="{StaticResource Text.FieldLabel}" Margin="0,14,0,6"/><ComboBox ItemsSource="{Binding NamingStrategies}" SelectedItem="{Binding SelectedNamingStrategy}" Style="{StaticResource ProductComboBoxStyle}"/><TextBlock Text="重名处理" Style="{StaticResource Text.FieldLabel}" Margin="0,14,0,6"/><ComboBox ItemsSource="{Binding ConflictPolicies}" SelectedItem="{Binding SelectedConflictPolicy}" Style="{StaticResource ProductComboBoxStyle}"/><TextBlock Text="自定义命名模板" Style="{StaticResource Text.FieldLabel}" Margin="0,14,0,6"/><TextBox Text="{Binding NamingTemplate, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ProductTextBoxStyle}"/><TextBlock Text="支持 {video-name}、{language}、{layout}、{date}" Style="{StaticResource Text.Meta}" Margin="0,5,0,0"/><TextBlock Text="自定义输出目录" Style="{StaticResource Text.FieldLabel}" Margin="0,14,0,6"/><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding CustomOutputDirectory, Mode=OneWay}" Style="{StaticResource ProductTextBoxStyle}" IsReadOnly="True"/><Button Grid.Column="2" Content="选择目录" Click="SelectOutputDirectory_OnClick" Style="{StaticResource SecondaryButtonStyle}"/></Grid><Border Background="{StaticResource Brush.Surface.Info}" BorderBrush="#D6DEFF" BorderThickness="1" CornerRadius="{StaticResource Radius.Control}" Padding="12" Margin="0,16,0,0"><TextBlock Text="{Binding PublicationPreview, StringFormat=输出预览：{0}}" Foreground="{StaticResource Brush.Text.Secondary}" TextWrapping="Wrap"/></Border></StackPanel></Border>
            </StackPanel>
        </Grid></ScrollViewer>
        <Border Grid.Row="2" Style="{StaticResource PageFooterStyle}" Padding="30,12"><Grid><TextBlock Text="设置保存在当前 Windows 用户目录，不会写入视频文件夹。" Foreground="{StaticResource Brush.Text.Secondary}" VerticalAlignment="Center"/><Button Content="保存所有设置" Click="Save_OnClick" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right"/></Grid></Border>
    </Grid>
</UserControl>
